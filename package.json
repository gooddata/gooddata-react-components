{
  "name": "@gooddata/data-layer",
  "version": "0.0.1",
  "description": "",
  "main": "dist/index.js",
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "dependencies": {
    "es6-promise": "^4.0.5",
    "gooddata": "^1.3.0",
    "lodash": "^4.17.4"
  },
  "devDependencies": {
    "@types/jest": "^18.1.1",
    "@types/lodash": "^4.14.52",
    "@types/node": "^7.0.8",
    "jest": "19.0.1",
    "ts-jest": "19.0.0",
    "tslint": "^4.4.2",
    "typescript": "^2.1.6"
  },
  "scripts": {
    "build": "tsc --outDir dist",
    "test": "jest",
    "watchTest": "jest --watch",
    "dev": "rm -rf dist && tsc --outDir dist --watch",
    "prepublish": "rm -rf dist && tsc --outDir dist",
    "validate": "tslint -c tslint.json 'src/**/*.ts' *.ts"
  },
  "author": "",
  "license": "ISC",
  "jest": {
    "transform": {
      ".(ts|tsx)": "<rootDir>/node_modules/ts-jest/preprocessor.js"
    },
    "testRegex": "(/__tests__/.*|\\.(test|spec))\\.(ts)$",
    "moduleFileExtensions": [
      "ts",
      "js"
    ]
  }
}
